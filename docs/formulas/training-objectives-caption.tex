\documentclass[border=0pt]{standalone}
\usepackage{amsmath}
\usepackage{xcolor}
\begin{document}
\color[HTML]{64748B}
\sffamily\fontsize{9}{11}\selectfont
\setlength{\parindent}{0pt}
\begin{minipage}{315mm}
Symbols: $\mathbf{x}$ is a counted ECFP vector, $\mathbf{x}_{\log}=\log(1+\mathbf{x})$ is the sparse model input, and $\widehat{\mathbf{x}}_{\log}$ is the decoded reconstruction. $\mathbf{d}$ and $\widehat{\mathbf{d}}$ are molecule-derived descriptor targets and predictions. $\mathbf{z}=E(\mathbf{x}_{\log})$ is the encoder embedding and $D(\mathbf{z})$ is the decoder output. $T_c(\mathbf{x}_i,\mathbf{x}_j)$ is counted Tanimoto similarity between molecules $i$ and $j$, and $\cos(\mathbf{z}_i,\mathbf{z}_j)$ is latent cosine similarity. $\mathcal{C}_i$ is the sampled candidate set for anchor $i$; $p^z_{ic}$ is the latent softmax probability for candidate $c$; $c_i^\star$ is the highest-Tanimoto candidate; $w_i$ is the top-2 counted-Tanimoto gap; and $m_i$ masks anchors with $w_i\le\gamma$. Tanimoto terms are averaged over valid anchors with $w_i$ weights. $\mathcal{L}_{\mathrm{recon}}$ is weighted Huber reconstruction, $\mathcal{L}_{\mathrm{desc}}$ is descriptor MSE, and $\mathcal{L}_{\mathrm{tan}}$ is the sampled softmax cross-entropy Tanimoto geometry loss.
\end{minipage}
\end{document}
