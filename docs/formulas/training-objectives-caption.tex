\documentclass[border=0pt]{standalone}
\usepackage{amsmath}
\usepackage{xcolor}
\begin{document}
\color[HTML]{64748B}
\sffamily\fontsize{9}{11}\selectfont
\setlength{\parindent}{0pt}
\begin{minipage}{315mm}
Symbols: $\mathbf{x}$ is a counted ECFP vector, $\mathbf{x}_{\log}=\log(1+\mathbf{x})$ is the sparse model input, and $\widehat{\mathbf{x}}_{\log}$ is the decoded reconstruction. $\mathbf{d}$ and $\widehat{\mathbf{d}}$ are molecule-derived descriptor targets and predictions. $\mathbf{z}=E(\mathbf{x}_{\log})$ is the encoder embedding and $D(\mathbf{z})$ is the decoder output. $T_c(i,j)$ is counted Tanimoto similarity between molecules $i$ and $j$; $\cos(\mathbf{z}_i,\mathbf{z}_j)$ is latent cosine similarity. $\Delta^T$ and $\Delta^z$ are pairwise metric and latent gaps; $m_{ijk}$ masks pairs with counted-Tanimoto gap at most $\gamma$. $\Delta^z/\tau_z$ is the latent logit; $\sigma(\Delta^T/\tau_T)$ is the soft metric target; $\tau_z$ and $\tau_T$ are temperatures. Tanimoto terms are averaged over valid pairs with $|\Delta^T|$ gap weights. $\mathcal{L}_{\mathrm{recon}}$ is weighted Huber reconstruction, $\mathcal{L}_{\mathrm{desc}}$ is descriptor MSE, and $\mathcal{L}_{\mathrm{tan}}$ is the pairwise logistic Tanimoto geometry loss.
\end{minipage}
\end{document}
